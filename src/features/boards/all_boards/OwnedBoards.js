import { PageHeader } from "antd";
import React from "react";

export default function OwnedBoards() {
  const boards = [{ name: "" }];
  return (
    <div>
      <PageHeader title="Title" backIcon={null} />
    </div>
  );
}
